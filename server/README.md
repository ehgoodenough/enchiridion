# Enchiridion (server-side) #

### Getting Started ###

Have node v8+ and npm v5+.

```
node --version
npm --version
```

Install the serverless framework.

```
npm install --global serverless
```

Deploy the service.

```
npm install
npm run deploy
```
